
Write a C++ program using enum to classify a student’s marks into grades.
Define an enum Grade with the values: FAIL, PASS, MERIT, DISTINCTION.
Take marks as input (0–100) from the user.
Determine the grade based on the marks:

<40 → FAIL
40–59 → PASS
60–79 → MERIT
80–100 → DISTINCTION
Use the enum to store the grade and print a message according to the grade.



#include<iostream>
#include<string>
using namespace std;

enum check {FAIL=0, PASS=1, MERIT=2, DISTINCTION=3};

int main() {
    int num;
    cout << "Enter the number : ";
    cin >> num;

    int grade; // using int variable similar to your original code

    // Corrected conditions: compare 'num', not 'grade'
    if(num < 40) {
        grade = 0;
    }
    else if(num >= 40 && num <= 59) {
        grade = 1;
    }
    else if(num >= 60 && num <= 79) {
        grade = 2;
    }
    else {
        grade = 3;
    }

    // Print message based on grade
    switch(grade) {
        case 0: cout << "Sorry you are failed!"; break;
        case 1: cout << "Ohh you are passed"; break;
        case 2: cout << "Good Job You got merit"; break;
        case 3: cout << "Congratulation for distinction"; break;
    }

    return 0;
}
